<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}Vidéothèque!{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        <div class="container">
            <div id="header" class="jumbotron">
                <h1>Vidéothèque en ligne</h1>
            </div>
            <div class="row">
                <nav class="navbar navbar-fixed-top navbar-inverse">
                    <div class="container">
                        <div class="navbar-header">
                            <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                                <span class="sr-only">Toggle navigation</span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                                <span class="icon-bar"></span>
                            </button>
                            <a class="navbar-brand" href="#">Vidéothèque</a>
                        </div>
                        <div id="navbar" class="collapse navbar-collapse">
                            <ul class="nav navbar-nav">
                                <li class="active"><a href="{{ path("film_index") }}">Accueil</a></li>
                                <li><a href="{{ path("film_new") }}">Création Film</a></li>
                                <li><a href="{{ path("category_new") }}">Création Catégorie</a></li>
                            </ul>
                        </div><!-- /.nav-collapse -->
                    </div><!-- /.container -->
                </nav>
                <div id="content">
                    {% block body %}
                    {% endblock %}
                </div>
            </div>
            <footer>Responsable du site : {{ webmaster }}</footer>
        </div>
        {% block javascripts %}{% endblock %}
    </body>
</html>
